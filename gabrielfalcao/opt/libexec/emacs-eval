#!/usr/bin/env bash

export IFS=$'\n'
set -umeTE
set +f
set -o pipefail

declare -a argv=($@)
declare -i argc=${#argv[@]}

libexec="$(dirname $BASH_SOURCE[0])/emacs"

. "$libexec"

unset IFS

client --eval "${argv[@]}"
